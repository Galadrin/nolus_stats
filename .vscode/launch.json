{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Exécuter le script : dev",
            "request": "launch",
            "command": "yarn run dev",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env.local"
        }
    ]
}